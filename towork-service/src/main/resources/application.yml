server:
  port: 8089

mybatis:
  type-aliases-package: com.java.toworkservice.entity
#sql��ӡ
logging:
  level:
    com:
      example:
        qhit:
          exam:
            mapper: DEBUG
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8888/eureka
  instance:
    # ������getHostname��ȡʵ����hostnameʱ������ip������host����
    prefer-ip-address: true
    lease-expiration-duration-in-seconds: 90
    lease-renewal-interval-in-seconds: 30
    ip-address: 127.0.0.1
spring:
  datasource:
    password: 123456
    username: root
    url: jdbc:mysql://127.0.0.1:3306/towork?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&useSSL=false
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    password:
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 5
        min-idle: 0
    timeout: 10000
  application:
    name: towork-server
